import React from 'react';
import type {MetaFunction} from "@remix-run/node";

export const meta: MetaFunction = () => {return [{ title: "Import" }]}

export default function Import() {
    return (
        <div>

        </div>
    )
}
