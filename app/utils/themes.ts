import type { ThemeOptions } from "@mui/material"

export default {
    dark: {
        palette: {
            mode: "dark"
        }
    },
    light: {
        palette: {
            mode: "light"
        }
    }
} as { [index: string]: ThemeOptions }
